import { FilterSubscriptionsPipe } from './filter-subscriptions.pipe';

describe('FilterSubscriptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubscriptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
